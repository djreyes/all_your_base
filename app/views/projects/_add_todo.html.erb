<li class="form">
  <%= form_for [@project, @todo], method: :post, remote: true do |f| %>
    <p>
      Add Todo
      <%= f.text_field :name %>
      <%= f.hidden_field :created_by_id, value: current_user.id %>
    </p>
    <%= f.submit class: "btn btn-primary"%>
  <% end %>
</li>