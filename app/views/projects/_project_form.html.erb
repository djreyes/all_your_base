<%= form_for project, method: :post, remote: true do |f| %>
  <p>
    <%= f.label :name %>
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.label :description %>
    <%= f.text_field :description %>
  </p>

  <%= f.submit :class => "btn btn-primary add-project" %>
<% end %>
