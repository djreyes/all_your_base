<h2>Create a Todo</h2>
<%= form_for [@project, @todo] do |f| %>
  <p>
    <%= f.label :name %>
    <%= f.text_field :name %>
  </p>

  <%= f.hidden_field :created_by_id, value: current_user.id %>

  <%= f.submit %>
<% end %>